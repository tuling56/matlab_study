clear all;
P = [-3 -2 -2 0 0 0 0 2 2 3; 0 1 -1 2 1 -1 -2 1 -1 0];  %样本数据
net = newlvq(P,4,[.6 .4]);        %创建一个学习向量化LVQ网络
w1=net.IW{1,1}                    %确认第一层的权重矩阵的初始值
w2=net.LW{2,1}                    %确认第二层的权重矩阵的初始值
Y = sim(net,P)                    %网络训练
